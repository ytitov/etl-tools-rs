initSidebarItems({"fn":[["data_output_shutdown","the purpose of this is to drop the sender, then wait on the receiver to finish. the drop signals that it should finish, and waiting on the JoinHandle is to allow it to finish its own work.  Of course if there are copies of the sender floating around somewhere, this may cause some issues.  This is only used in the job handler, so might be a good idea to just add it to the trait"]],"mod":[["job","Create pipelines linking [etl_core::datastore::DataSource]s together; Provides state management based on a job id, responsible for generating and running pipelines, including any run-time configurations"],["job_manager","and records each step.  A successful job with the same job id will not run more than once"],["transform_store",""]]});